{
  "name": "riscv-compiler",
  "image": "rust:latest",
  "postCreateCommand": "apt-get update && apt-get install -y binutils-riscv64-linux-gnu qemu-user && rustup component add rustfmt",
  "customizations": {
    "vscode": {
      "extensions": ["rust-lang.rust-analyzer"]
    }
  },
  "mounts": [
    "source=riscv-compiler-target,target=/workspaces/risc-v_compiler/target,type=volume",
    "source=cargo-registry-cache,target=/usr/local/cargo/registry,type=volume"
  ]
}